/**
 * Legacy compatibility re-export layer.
 *
 * Canonical path policy:
 * - New or edited imports should use `components/features/heatmap/*`.
 * - Keep this barrel for backwards compatibility while older imports are phased out.
 */
export * from '../features/heatmap'
